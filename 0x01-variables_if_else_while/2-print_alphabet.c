#include <stdio.h>
/**
 * main - Entry point
 * Description: print alphabets in lowercases'
 * Return: Always 0
 */
int main()
{
char alpha = 'a';
For (alpha = ‘a’; alpha <= ‘z’;alpha++)
{
	putchar(alpha);
}
putchar("\n");
return (0);
}

